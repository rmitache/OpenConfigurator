import { Injectable } from '@angular/core';

@Injectable()
export class DialogManagerService {

    public CreateDialog(title: string) {
        // should return the reference to a dialog 
        // the dialog should already be added to the page, and calling show/hide on it should work 
    }
}


 
